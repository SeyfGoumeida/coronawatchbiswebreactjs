import React from "react";
import { shallow } from "enzyme";
import Robots_youtube from "./Robots_youtube";

describe("Robots youtube content Testing", ()=>{
    let wrapper;
    beforeEach(()=> {
        wrapper = shallow(<Robots_youtube/>);
    });
    test("render buttons", () => {
        //console.log(wrapper.debug());

        
        
    });

});